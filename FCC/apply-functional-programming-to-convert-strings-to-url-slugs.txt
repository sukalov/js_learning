// Only change code below this line
function urlSlug(title) {
const reduced = title.trim(/s+/)
                     .toLowerCase()
                     .split(/\s+/)
return reduced.join('-')
}
// Only change code above this line
console.log(urlSlug("Winter Is  Coming"))